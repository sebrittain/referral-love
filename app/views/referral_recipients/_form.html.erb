<%= form_for(@referral_recipient) do |f| %>
  <% if @referral_recipient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@referral_recipient.errors.count, "error") %> prohibited this referral_recipient from being saved:</h2>

      <ul>
      <% @referral_recipient.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :website %><br />
    <%= f.text_field :website %>
  </div>
    <div class="actions">
      <div class="btn-custom">
        <%= submit_tag("Get Referral") %>
      </div>
    </div>
<% end %>
