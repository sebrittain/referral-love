<h1></h1>
  <div id="teaser">
    <div class="wrap">
      <div id="image"></div>
      <div class="box">
        <h2>Listing referral_recipients</h2>
        <p></p>
      </div>
    </div>
  </div>
<table>
  <tr>
    <th>Website</th>
    <th>Referral code</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @referral_recipients.each do |referral_recipient| %>
  <tr>
    <td><%= referral_recipient.website %></td>
    <td><%= referral_recipient.referral_code %></td>
    <td><%= link_to 'Show', referral_recipient %></td>
    <td><%= link_to 'Edit', edit_referral_recipient_path(referral_recipient) %></td>
    <td><%= link_to 'Destroy', referral_recipient, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Referral recipient', new_referral_recipient_path %>
